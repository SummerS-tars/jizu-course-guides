| 实验编号 | 实现能力 | 期末可能的考题类型 | 来源 |
| :--- | :--- | :--- | :--- |
| **Lab 4** | 1. 实现 6 条特权指令（CSRRW/RS/RC 等）。<br>2. 支持核心 CSR 寄存器（mstatus, mtvec, mepc, mcause, satp 等）。<br>3. 处理 CSR 读写掩码（Mask）机制。 | 1. CSR 寄存器功能与作用。<br>2. WARL（写任意值，读合法值）字段机制。<br>3. 特权模式下的状态位控制。 | [1], [2], [3], [4], [5] |
| **Lab 5** | 1. 实现 M/U 特权级切换及 ECALL/MRET 指令。<br>2. 实现 **Sv39 MMU**，支持三级页表遍历（Walk）。<br>3. 实现取指与访存的统一虚拟地址翻译。 | 1. **页表遍历计算**（虚拟地址转物理地址）。<br>2. **SATP 寄存器**结构（Mode, ASID, PPN）。<br>3. 精确异常下的流水线冲刷（Flush）策略。 | [6], [7], [8], [9], [4], [10] |
| **Lab 6** | 1. 处理同步异常（非法指令、地址不对齐、系统调用）。<br>2. 处理异步中断（时钟、软件、外部中断）。<br>3. 维护中断使能（MIE/MPIE）与优先级。 | 1. **异常/中断处理流程**（写寄存器 + 跳转）。<br>2. **SFENCE.VMA 指令**与 TLB 一致性管理。<br>3. mcause 编码含义与中断屏蔽机制。 | [11], [12], [13], [14], [15], [10], [16] |

这些 Lab 的实践共同构成了期末考试中关于“特权架构”与“存储管理”的核心考点 [17, 18]。

关于这些实验的实现细节或具体的页表计算方法，你是否需要我举例说明？